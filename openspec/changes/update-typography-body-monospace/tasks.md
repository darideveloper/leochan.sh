## 1. Implementation
- [x] 1.1 Update `Layout.astro` global styles to apply the default monospace font stack (JetBrains Mono, Courier New, monospace) to `p` tags and descriptions.
- [x] 1.2 Update Tailwind configuration `fontFamily` if a specific `font-mono` utility needs to be customized.
- [x] 1.3 Review and apply the monospace font to descriptions in `src/components/home.astro`.
- [x] 1.4 Review and apply the monospace font to project descriptions in `src/components/projects.astro`.
- [x] 1.5 Review and apply the monospace font to text in `src/components/contact.astro` and `src/components/footer.astro`.
- [x] 1.6 Verify that Montserrat is still used correctly for UI elements (like nav links).
