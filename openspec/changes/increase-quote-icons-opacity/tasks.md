# Tasks: Increase Quote Icons Opacity

## Implementation Tasks

- [x] **1. Modify `src/components/home.astro` - Skills Section**
    - [x] Update the decorative `<span>` elements for quotes.
    - [x] Change `opacity-10` to `opacity-40` for both icons (lines 160 and 164).

## Validation Tasks

- [x] **1. Visual Verification**
    - [x] Confirm quote icons are visibly darker/more opaque.
- [x] **2. Build Verification**
    - [x] Run `npm run build` to ensure no build regressions.
- [x] **3. Spec Validation**
    - [x] Run `openspec validate increase-quote-icons-opacity --strict`.
