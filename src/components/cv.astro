---
import TypingText from "@/React/TypingText";
import cvData from "@/data/cv";

const {
  name,
  role,
  contact,
  aboutMe,
  technicalSkills,
  experience,
  education,
  projects,
  aeronautical,
  languages,
  interests,
} = cvData;
---

<section id="cv" class="py-24 px-4 sm:px-6 lg:px-8 max-w-7xl mx-auto scroll-mt-24">
  <div class="flex flex-col gap-12">
    <!-- CV Header -->
    <header class="cyber-glass p-8 rounded-3xl border border-white/10 flex flex-col md:flex-row justify-between items-start md:items-center gap-6 relative overflow-hidden">
      <div class="z-10">
        <h2 class="text-2xl md:text-5xl font-vcr text-[var(--white)] tracking-tight">
          <TypingText client:load text={name} speed={50} />
        </h2>
        <p class="text-xl md:text-2xl text-[var(--sec)] font-vcr mt-2 shiny-sec">
          <TypingText client:load text={role} speed={40} delay={1500} />
        </p>
      </div>
      <div class="flex flex-col gap-2 text-[var(--white-icon)] font-mono text-sm z-10">
        <a href={`mailto:${contact.email}`} class="hover:text-[var(--primary)] transition-colors flex items-center gap-2">
          <span class="text-[var(--sec)]">[MAIL]</span> {contact.email}
        </a>
        <a href={`tel:${contact.phone.replace(/\s+/g, '')}`} class="hover:text-[var(--primary)] transition-colors flex items-center gap-2">
          <span class="text-[var(--sec)]">[TEL]</span> {contact.phone}
        </a>
        <a href={contact.linkedin} target="_blank" class="hover:text-[var(--primary)] transition-colors flex items-center gap-2">
          <span class="text-[var(--sec)]">[LINK]</span> LinkedIn Profile
        </a>
        <div class="flex items-center gap-2">
          <span class="text-[var(--sec)]">[DRIVE]</span> {contact.drivingLicense}
        </div>
      </div>
      
      <!-- Background Glow Effect -->
      <div class="absolute -right-20 -top-20 w-64 h-64 bg-[var(--primary)] opacity-10 blur-[100px] pointer-events-none"></div>
    </header>

    <div class="grid grid-cols-1 lg:grid-cols-[1fr_2fr] gap-8">
      <!-- Left Column -->
      <aside class="flex flex-col gap-8">
        <!-- About Me -->
        <div class="cyber-glass p-6 rounded-2xl border border-white/10">
          <h3 class="text-xl font-vcr text-[var(--white)] mb-4 flex items-center gap-2">
            <span class="w-2 h-2 bg-[var(--primary)] rounded-full animate-pulse"></span>
            ABOUT ME
          </h3>
          <p class="text-[var(--white-icon)] text-sm leading-relaxed text-justify">
            {aboutMe}
          </p>
        </div>

        <!-- Technical Skills -->
        <div class="cyber-glass p-6 rounded-2xl border border-white/10">
          <h3 class="text-xl font-vcr text-[var(--white)] mb-6 flex items-center gap-2">
             <span class="w-2 h-2 bg-[var(--primary)] rounded-full animate-pulse"></span>
             TECH STACK
          </h3>
          <div class="space-y-6">
            {technicalSkills.map((cat) => (
              <div>
                <h4 class="text-[var(--sec)] font-vcr text-xs mb-3 tracking-widest">{cat.category}</h4>
                <ul class="space-y-3">
                  {cat.skills.map((skill) => (
                    <li class="flex flex-col gap-1">
                      <span class="text-[var(--white)] text-sm font-semibold">{skill.name}</span>
                      <span class="text-[var(--white-icon)] text-xs">{skill.details}</span>
                    </li>
                  ))}
                </ul>
              </div>
            ))}
          </div>
        </div>

        <!-- Languages -->
        <div class="cyber-glass p-6 rounded-2xl border border-white/10">
          <h3 class="text-xl font-vcr text-[var(--white)] mb-4 flex items-center gap-2">
            <span class="w-2 h-2 bg-[var(--primary)] rounded-full animate-pulse"></span>
            LANGUAGES
          </h3>
          <div class="space-y-4">
            {languages.map((lang) => (
              <div class="flex justify-between items-start gap-4">
                <span class="text-[var(--white)] text-sm font-vcr shrink-0">{lang.name}</span>
                <span class="text-[var(--white-icon)] text-xs font-mono text-right">{lang.level}</span>
              </div>
            ))}
          </div>
        </div>

        <!-- Interests -->
        <div class="cyber-glass p-6 rounded-2xl border border-white/10">
          <h3 class="text-xl font-vcr text-[var(--white)] mb-4 flex items-center gap-2">
            <span class="w-2 h-2 bg-[var(--primary)] rounded-full animate-pulse"></span>
            INTERESTS
          </h3>
          <div class="flex flex-wrap gap-2">
            {interests.map((interest) => (
              <span class="text-[var(--white-icon)] text-xs border border-white/5 bg-white/5 px-3 py-1.5 rounded-full hover:border-[var(--sec)] transition-colors duration-300">
                {interest}
              </span>
            ))}
          </div>
        </div>
      </aside>

      <!-- Right Column -->
      <main class="flex flex-col gap-8">
        <!-- Professional Experience -->
        <div class="cyber-glass p-8 rounded-2xl border border-white/10">
          <h3 class="text-2xl font-vcr text-[var(--white)] mb-8 flex items-center gap-3">
            <span class="w-3 h-3 bg-[var(--sec)] rounded-full"></span>
            EXPERIENCE
          </h3>
          <div class="space-y-10 pl-8 border-l border-white/10">
            {experience.map((exp) => (
              <div class="relative">
                <div class="absolute -left-[37px] top-1.5 w-4 h-4 rounded-full bg-[var(--background)] border-2 border-white/10"></div>
                <div class="flex flex-col sm:flex-row sm:items-center justify-between mb-2 gap-2">
                  <h4 class="text-[var(--white)] font-bold text-lg">{exp.company}</h4>
                  <span class="text-[var(--white-icon)] font-mono text-xs px-3 py-1 bg-white/5 rounded-lg border border-white/10">{exp.date}</span>
                </div>
                <p class="text-[var(--primary-light)] font-vcr text-sm tracking-wide">{exp.role}</p>
              </div>
            ))}
          </div>
        </div>

        <!-- Education -->
        <div class="cyber-glass p-8 rounded-2xl border border-white/10">
          <h3 class="text-2xl font-vcr text-[var(--white)] mb-8 flex items-center gap-3">
            <span class="w-3 h-3 bg-[var(--sec)] rounded-full"></span>
            EDUCATION
          </h3>
          <div class="space-y-12 pl-8 border-l border-white/10">
            {education.map((edu) => (
              <div class="relative">
                <div class="absolute -left-[37px] top-1.5 w-4 h-4 rounded-full bg-[var(--background)] border-2 border-white/10"></div>
                <div class="flex flex-col sm:flex-row sm:items-center justify-between mb-3 gap-2">
                  <h4 class="text-[var(--white)] font-bold text-lg">{edu.institution}</h4>
                  <span class="text-[var(--white-icon)] font-mono text-xs px-3 py-1 bg-white/5 rounded-lg border border-white/10">{edu.date}</span>
                </div>
                <ul class="space-y-2">
                  {edu.details.map((detail) => (
                    <li class="text-[var(--white-icon)] text-sm flex items-start gap-2">
                      <span class="text-[var(--sec)] mt-1 opacity-50">▹</span>
                      <span>{detail}</span>
                    </li>
                  ))}
                </ul>
              </div>
            ))}
          </div>
        </div>

        <!-- Project Management -->
        <div class="cyber-glass p-8 rounded-2xl border border-white/10">
          <h3 class="text-2xl font-vcr text-[var(--white)] mb-8 flex items-center gap-3">
            <span class="w-3 h-3 bg-[var(--sec)] rounded-full"></span>
            PROJECTS
          </h3>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            {projects.map((project) => (
              <div class="bg-white/5 border border-white/10 p-5 rounded-xl hover:bg-white/10 transition-colors duration-300">
                <h4 class="text-[var(--white)] font-bold mb-4 text-sm uppercase tracking-tight leading-tight">{project.title}</h4>
                <ul class="space-y-2">
                  {project.tasks.map((task) => (
                    <li class="text-[var(--white-icon)] text-xs flex items-start gap-2">
                      <span class="text-[var(--primary)] shrink-0">•</span>
                      <span>{task}</span>
                    </li>
                  ))}
                </ul>
              </div>
            ))}
          </div>
        </div>

        <!-- Aeronautical Skills -->
        <div class="cyber-glass p-8 rounded-2xl border border-white/10">
          <h3 class="text-2xl font-vcr text-[var(--white)] mb-8 flex items-center gap-3">
            <span class="w-3 h-3 bg-[var(--sec)] rounded-full"></span>
            AERONAUTICS
          </h3>
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
            {aeronautical.map((item) => (
              <div class="bg-white/5 border border-white/10 p-5 rounded-xl hover:bg-white/10 transition-colors duration-300 flex items-center justify-center min-h-[100px] text-center">
                <p class="text-[var(--white)] text-sm font-medium leading-relaxed">{item}</p>
              </div>
            ))}
          </div>
        </div>
      </main>
    </div>
  </div>
</section>

<style>
  .shiny-sec {
    background: linear-gradient(
      135deg,
      var(--primary) 25%,
      var(--primary-light) 50%,
      var(--primary) 75%
    );
    background-size: 400% 100%;
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    animation: shine 3s linear infinite;
  }

  @keyframes shine {
    0% {
      background-position: 100% 50%;
    }
    30%,
    70% {
      background-position: 0% 50%;
    }
  }
</style>
